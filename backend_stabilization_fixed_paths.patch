diff --git a/backend/src/config.py b/backend/src/config.py
--- a/backend/src/config.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/config.py	2026-04-06 21:24:52.213355251 +0000
@@ -223,6 +223,13 @@
 
 class ModelAction(str, Enum):
     REPLACE_CHART = "replace_chart"
+    POLISH = "polish"
+    CORRECT = "correct"
+    TRANSLATE = "translate"
+    EXPAND = "expand"
+    SHORTEN = "shorten"
+    SIMPLIFY = "simplify"
+    SPECIFY = "specify"
     CUSTOM = "custom"
 
 
diff --git a/backend/src/database.py b/backend/src/database.py
--- a/backend/src/database.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/database.py	2026-04-06 21:24:52.210992490 +0000
@@ -1,6 +1,7 @@
+from fastapi import HTTPException
 from sqlalchemy import create_engine
 from sqlalchemy.orm import sessionmaker, declarative_base
-from fastapi import HTTPException
+
 from src.config import settings
 
 DATABASE_ENABLED = bool(settings.USE_DATABASE)
@@ -25,7 +26,7 @@
         f"{settings.POSTGRES_HOST}:{settings.POSTGRES_PORT}/"
         f"{settings.POSTGRES_DB}"
     )
-    engine = create_engine(DATABASE_URL, echo=True)  # echo=True для логов SQL
+    engine = create_engine(DATABASE_URL, echo=False, pool_pre_ping=True)
     SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
 
 Base = declarative_base()
diff --git a/backend/src/main.py b/backend/src/main.py
--- a/backend/src/main.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/main.py	2026-04-06 21:24:52.211420174 +0000
@@ -1,7 +1,11 @@
+import logging
+
 from fastapi import FastAPI
 from fastapi.middleware.cors import CORSMiddleware
-import logging
 
+from src.config import settings
+from src.database import engine, Base
+from src.preload import preload_models
 from src.routes import (
     file_routes,
     presentation_routes,
@@ -9,8 +13,7 @@
     auth_routes,
     kandinsky_routes,
 )
-from src.preload import preload_models
-from src.config import settings
+from src.schemas.user_schemas import User, Presentation
 
 app = FastAPI(
     title="API Documentation",
@@ -20,7 +23,6 @@
     docs_url="/docs",
 )
 
-# Настройка CORS (без wildcard в production)
 origins = [o.strip() for o in settings.CORS_ORIGINS.split(",") if o.strip()]
 
 app.add_middleware(
@@ -40,9 +42,11 @@
 )
 
 
-# Preload models at startup
 @app.on_event("startup")
 async def startup_event():
+    if engine is not None:
+        Base.metadata.create_all(bind=engine)
+
     if not settings.PRELOAD_MODELS:
         logging.info("Startup: model preload disabled (PRELOAD_MODELS=false)")
         return
diff --git a/backend/src/modules/models/slide_content_generator.py b/backend/src/modules/models/slide_content_generator.py
--- a/backend/src/modules/models/slide_content_generator.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/modules/models/slide_content_generator.py	2026-04-06 21:24:52.209151811 +0000
@@ -65,7 +65,7 @@
                 return model_api_utils.get_content(resp)
             except Exception:
                 if attempt < retry - 1:
-                    time.sleep(1 + attempt * 2)
+                    time.sleep(0.5 * (attempt + 1))
                     continue
                 raise
 
@@ -114,9 +114,14 @@
             or f"### {slide_title}\n\n* Данные для этого раздела отсутствуют"
         )
 
-        chart_blocks = self.generate_charts_with_llm(
-            slide_id, slide_title, slide_task, topic, retrieved, max_tokens=600
-        )
+        chart_blocks = []
+        try:
+            chart_blocks = self.generate_charts_with_llm(
+                slide_id, slide_title, slide_task, topic, retrieved, max_tokens=600
+            )
+        except Exception as e:
+            logging.warning(f"[Slide {slide_id}] chart generation failed: {e}")
+            chart_blocks = []
 
         if chart_blocks:
             if self._should_chart_only(slide_title, slide_task):
diff --git a/backend/src/routes/auth_routes.py b/backend/src/routes/auth_routes.py
--- a/backend/src/routes/auth_routes.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/routes/auth_routes.py	2026-04-06 21:24:52.209646980 +0000
@@ -1,3 +1,5 @@
+import secrets
+
 from fastapi import APIRouter, Response, HTTPException, Cookie
 from pydantic import BaseModel, EmailStr
 
@@ -20,9 +22,11 @@
     if not data.guest or data.password != "guest":
         raise HTTPException(status_code=401, detail="Доступ разрешён только для гостевого входа")
 
+    session_token = secrets.token_urlsafe(32)
+
     response.set_cookie(
         key=COOKIE_NAME,
-        value="enabled",
+        value=session_token,
         httponly=True,
         max_age=60 * 60 * 24,
         secure=False,
@@ -41,7 +45,7 @@
 
 @router.get("/me")
 def me(guest_mode: str | None = Cookie(default=None, alias=COOKIE_NAME)):
-    if guest_mode != "enabled":
+    if not guest_mode:
         raise HTTPException(status_code=401, detail="Гостевой режим не активирован")
 
     return {
diff --git a/backend/src/routes/presentation_routes.py b/backend/src/routes/presentation_routes.py
--- a/backend/src/routes/presentation_routes.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/routes/presentation_routes.py	2026-04-06 21:24:52.209973881 +0000
@@ -5,18 +5,20 @@
 from uuid import uuid4
 from collections import defaultdict
 
-from fastapi import APIRouter, Depends, File, Form, HTTPException, Request, Response, UploadFile, WebSocket, WebSocketDisconnect
+from fastapi import APIRouter, Depends, File, Form, HTTPException, Request, Response, UploadFile, WebSocket, WebSocketDisconnect, Cookie
+from fastapi.concurrency import run_in_threadpool
 from fastapi.responses import StreamingResponse
 from sqlalchemy.orm import Session
 
+from src.config import settings
 from src.database import get_db
+from src.routes.auth_routes import COOKIE_NAME
 from src.schemas.presentation_schema import SavePresentationSchema
 from src.schemas.presentation_schemas import EditSlideInSchema, GeneratePresInSchema
 from src.schemas.user_schemas import Presentation
-from src.services.convert_file_service import convert_file
+from src.services.convert_file_service import convert_uploaded_file_sync
 from src.services.model_service import edit_one_slide, generate_presentation
 from src.services.rust_sidecar_client import format_stream_chunk
-from src.config import settings
 
 router = APIRouter(prefix="/presentation")
 
@@ -63,13 +65,26 @@
 
 
 @router.get("/my-presentations")
-def my_presentations(db: Session = Depends(get_db)):
-    return db.query(Presentation).all()
+def my_presentations(
+    guest_mode: str | None = Cookie(default=None, alias=COOKIE_NAME),
+    db: Session = Depends(get_db),
+):
+    if not guest_mode:
+        raise HTTPException(status_code=401, detail="Guest session required")
+
+    return db.query(Presentation).filter_by(owner_token=guest_mode).all()
 
 
 @router.delete("/presentations/{presentation_id}")
-def delete_presentation(presentation_id: str, db: Session = Depends(get_db)):
-    pres = db.query(Presentation).filter_by(id=presentation_id).first()
+def delete_presentation(
+    presentation_id: str,
+    guest_mode: str | None = Cookie(default=None, alias=COOKIE_NAME),
+    db: Session = Depends(get_db),
+):
+    if not guest_mode:
+        raise HTTPException(status_code=401, detail="Guest session required")
+
+    pres = db.query(Presentation).filter_by(id=presentation_id, owner_token=guest_mode).first()
     if not pres:
         raise HTTPException(status_code=404, detail="Presentation not found")
 
@@ -79,23 +94,32 @@
 
 
 @router.post("/save-presentation")
-def save_presentation(data: SavePresentationSchema, db: Session = Depends(get_db)):
+def save_presentation(
+    data: SavePresentationSchema,
+    guest_mode: str | None = Cookie(default=None, alias=COOKIE_NAME),
+    db: Session = Depends(get_db),
+):
+    if not guest_mode:
+        raise HTTPException(status_code=401, detail="Guest session required")
+
     if not data.id:
         data.id = str(uuid4())
         pres = Presentation(
             id=data.id,
             user_id=None,
+            owner_token=guest_mode,
             title=data.title,
             content=data.content,
             theme=data.theme,
         )
         db.add(pres)
     else:
-        pres = db.query(Presentation).filter_by(id=data.id).first()
+        pres = db.query(Presentation).filter_by(id=data.id, owner_token=guest_mode).first()
         if not pres:
             pres = Presentation(
                 id=data.id,
                 user_id=None,
+                owner_token=guest_mode,
                 title=data.title,
                 content=data.content,
                 theme=data.theme,
@@ -124,10 +148,13 @@
     body = GeneratePresInSchema(text=text, model=model)
 
     await _broadcast_progress(client_id, "parsing", "started")
-    context = await convert_file(file)
+    content = await file.read()
+    context = await run_in_threadpool(
+        convert_uploaded_file_sync,
+        file.filename or "document",
+        content,
+    )
     await _broadcast_progress(client_id, "parsing", "completed")
-    await _broadcast_progress(client_id, "retrieval", "started")
-    await _broadcast_progress(client_id, "retrieval", "completed")
     await _broadcast_progress(client_id, "generation", "started")
 
     async def markdown_stream():
@@ -140,5 +167,5 @@
 
 @router.post("/edit", status_code=200)
 async def edit(body: EditSlideInSchema) -> Response:
-    model_res = edit_one_slide(body.text, body.slide, body.action, body.model)
+    model_res = edit_one_slide(body.text, body.slide.model_dump(), body.action, body.model)
     return Response(content=model_res, media_type="text/markdown")
diff --git a/backend/src/schemas/presentation_schemas.py b/backend/src/schemas/presentation_schemas.py
--- a/backend/src/schemas/presentation_schemas.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/schemas/presentation_schemas.py	2026-04-06 21:24:52.213854248 +0000
@@ -11,12 +11,9 @@
     model: str = ""
 
     @model_validator(mode="after")
-    def check_action(self) -> EditSlideInSchema:
-        print("Начинается валидация")
+    def normalize_model(self):
         if self.model not in settings.DEFAULT_MODEL_VALUES:
-            print(f"Модель {self.model} недопустима, используется стандартная модель")
             self.model = settings.DEFAULT_MODEL
-
         return self
 
 
@@ -30,7 +27,7 @@
     slide: SlideItem
 
     @model_validator(mode="after")
-    def check_action(self) -> EditSlideInSchema:
+    def validate_custom_prompt(self):
         if (self.action == ModelAction.CUSTOM) and not self.text.strip():
             raise ValueError("При кастомном запросе промпт не может быть пустым!")
 
diff --git a/backend/src/schemas/user_schemas.py b/backend/src/schemas/user_schemas.py
--- a/backend/src/schemas/user_schemas.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/schemas/user_schemas.py	2026-04-06 21:24:52.210602436 +0000
@@ -1,8 +1,11 @@
+from typing import Optional
+
+from pydantic import BaseModel, ConfigDict, EmailStr
 from sqlalchemy import Column, Integer, String, JSON, ForeignKey, Boolean
 from sqlalchemy.orm import relationship
+
 from src.database import Base
-from pydantic import BaseModel, ConfigDict, EmailStr
-from typing import Optional
+
 
 class User(Base):
     __tablename__ = "users"
@@ -18,18 +21,18 @@
     presentations = relationship("Presentation", back_populates="user", cascade="all, delete")
 
 
-# Если хочешь отдельную таблицу для презентаций:
 class Presentation(Base):
     __tablename__ = "presentations"
 
     id = Column(String, primary_key=True, index=True)
-    user_id = Column(Integer, ForeignKey("users.id"))
+    user_id = Column(Integer, ForeignKey("users.id"), nullable=True)
+    owner_token = Column(String, index=True, nullable=False)
     title = Column(String, nullable=False)
-    content = Column(JSON, default=[])  # сюда можно сохранять массив PlateSlide
+    content = Column(JSON, default=[])
     theme = Column(JSON, nullable=True)
-    
+
     user = relationship("User", back_populates="presentations")
-    
+
 
 class UserSchema(BaseModel):
     id: int
@@ -41,7 +44,7 @@
 
     model_config = ConfigDict(from_attributes=True)
 
-    
+
 class UpdateUserSchema(BaseModel):
     name: Optional[str]
     email: Optional[EmailStr]
diff --git a/backend/src/services/convert_file_service.py b/backend/src/services/convert_file_service.py
--- a/backend/src/services/convert_file_service.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/services/convert_file_service.py	2026-04-06 21:24:52.206946485 +0000
@@ -8,24 +8,29 @@
 from src.services.rust_sidecar_client import parse_document
 
 
-async def convert_file(file: UploadFile) -> str:
-    file_ext = file_utils.get_file_ext(file.filename)
+def convert_uploaded_file_sync(filename: str, content: bytes) -> str:
+    file_ext = file_utils.get_file_ext(filename)
 
     if file_ext not in markdown_parser.allowed_formats:
         raise HTTPException(status_code=400, detail="Неподдерживаемый формат")
 
-    with tempfile.NamedTemporaryFile(delete=False, suffix=f".{file_ext}") as tmp_file:
-        content = await file.read()
-        rust_markdown = parse_document(file.filename or "document", content)
-        if rust_markdown:
-            return rust_markdown
-        tmp_file.write(content)
-        tmp_file.flush()
-        tmp_file_path = tmp_file.name
+    rust_markdown = parse_document(filename or "document", content)
+    if rust_markdown:
+        return rust_markdown
 
+    tmp_file_path = None
     try:
-        md_text = markdown_parser.parse(tmp_file_path)
+        with tempfile.NamedTemporaryFile(delete=False, suffix=f".{file_ext}") as tmp_file:
+            tmp_file.write(content)
+            tmp_file.flush()
+            tmp_file_path = tmp_file.name
+
+        return markdown_parser.parse(tmp_file_path)
     finally:
-        os.unlink(tmp_file_path)
+        if tmp_file_path and os.path.exists(tmp_file_path):
+            os.unlink(tmp_file_path)
 
-    return md_text
+
+async def convert_file(file: UploadFile) -> str:
+    content = await file.read()
+    return convert_uploaded_file_sync(file.filename or "document", content)
diff --git a/backend/src/services/model_service.py b/backend/src/services/model_service.py
--- a/backend/src/services/model_service.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/services/model_service.py	2026-04-06 21:25:46.068831117 +0000
@@ -10,19 +10,18 @@
 """
 
 from __future__ import annotations
-import os
-from typing import AsyncGenerator, Generator
+
 import json
 import logging
-import asyncio
+import os
+from typing import Generator
 
 from fastapi import HTTPException
 
-from src.config import settings, model_settings, ModelAction
+from src.config import ModelAction, model_settings, settings
 from src.modules.models import FZ44RAGSearcher, SlideContentGenerator
 from src.modules.models.rag import DocumentProcessor, QdrantVectorDatabase
 from src.schemas.model_schemas import ClassifierOut, StructureOut
-from src.schemas.presentation_schemas import EditSlideInSchema
 from src.utils import json_utils, model_api_utils, text_utils
 
 
@@ -31,6 +30,10 @@
     logging.info("Logging initialized")
 
 
+def get_runtime_api_key() -> str:
+    return model_api_utils.get_api_key()
+
+
 class IntegratedPipeline:
     def __init__(
         self,
@@ -65,11 +68,11 @@
         logging.info("START PIPELINE")
         logging.info("=" * 60)
         logging.info("[STEP 1] Classifier")
-        clf, clf_raw = self.run_classifier(user_request)
+        clf, _ = self.run_classifier(user_request)
         audience = clf.label
         logging.info(f"Audience: {audience} (conf={clf.confidence:.2f})")
         logging.info("[STEP 2] Planner")
-        struct, planner_raw = self.run_planner(audience, project_context)
+        struct, _ = self.run_planner(audience, project_context)
         slides = [s.model_dump() for s in struct.slides]
         slide_structure = [
             {
@@ -88,13 +91,6 @@
             "slide_structure": slide_structure,
         }
         return gen.generate_presentation_content(data)
-        self.last_run_metadata = {
-            "pipeline_metadata": {
-                "classifier_output": clf.model_dump(),
-                "structure_output": slides,
-            },
-            "generation_metadata": gen.generation_metadata,
-        }
 
     def replace_chart(self, slide: dict, params: dict, slides: list) -> dict:
         title = slide.get("title", "")
@@ -238,101 +234,90 @@
         user_prompt: str,
         slide: dict,
         action: ModelAction,
-        slides={},
-        params={},
+        slides: list | None = None,
+        params: dict | None = None,
     ) -> dict:
-        """
-        Regenerates a single slide based on the provided action and parameters.
-        """
+        slides = slides or []
+        params = params or {}
+
         if action == ModelAction.REPLACE_CHART:
-            try:
-                edited_slide = self.replace_chart(slide, params, slides)
-                return edited_slide
-            except Exception as e:
-                return None, e
-        else:
-            try:
-                payload = {
-                    "slide_id": slide.get("slide_id"),
-                    "title": slide.get("title", ""),
-                    "content": slide.get("content", ""),
-                    "action": action,
-                    "params": params,
-                }
-                if action == "custom":
-                    payload["custom_prompt"] = user_prompt
+            return self.replace_chart(slide, params, slides)
+
+        payload = {
+            "slide_id": slide.get("slide_id"),
+            "title": slide.get("title", ""),
+            "content": slide.get("content", ""),
+            "action": action.value if hasattr(action, "value") else str(action),
+            "params": params,
+        }
+        if action == ModelAction.CUSTOM:
+            payload["custom_prompt"] = user_prompt
 
-                messages = [
+        messages = [
+            {
+                "role": "user",
+                "content": model_settings.SINGLE_SLIDE_EDIT_PROMPT
+                + "\nUserInput: "
+                + json.dumps(payload, ensure_ascii=False),
+            }
+        ]
+
+        resp = model_api_utils.call_model(
+            messages,
+            self.api_key,
+            model=self.llm_model,
+            max_tokens=600,
+            temperature=0.2,
+        )
+        raw = text_utils.pre_sanitize(model_api_utils.get_content(resp))
+        parsed = json_utils.extract_json_balanced(raw)
+
+        if not isinstance(parsed, dict):
+            resp2 = model_api_utils.call_model(
+                [
                     {
                         "role": "user",
-                        "content": model_settings.SINGLE_SLIDE_EDIT_PROMPT
-                        + "\nUserInput: "
-                        + json.dumps(payload, ensure_ascii=False),
+                        "content": model_settings.JSON_ONLY_PROMPT + "\n\n" + messages[0]["content"],
                     }
-                ]
-                resp = model_api_utils.call_model(
-                    messages,
-                    self.api_key,
-                    model=self.llm_model,
-                    max_tokens=600,
-                    temperature=0.2,
-                )
-                raw = text_utils.pre_sanitize(resp["choices"][0]["message"]["content"])
-                parsed = json_utils.extract_json_balanced(raw)
+                ],
+                self.api_key,
+                model=self.llm_model,
+                max_tokens=600,
+                temperature=0.2,
+            )
+            raw2 = text_utils.pre_sanitize(model_api_utils.get_content(resp2))
+            parsed = json_utils.extract_json_balanced(raw2)
+
+        if not isinstance(parsed, dict):
+            raise HTTPException(status_code=502, detail="Invalid JSON from edit model")
 
-                if not isinstance(parsed, dict):
-                    resp2 = model_api_utils.call_model(
-                        [
-                            {
-                                "role": "user",
-                                "content": "Previous reply was not valid JSON. Please REPLY with valid JSON only.\n\n"
-                                + messages[0]["content"],
-                            }
-                        ],
-                        self.api_key,
-                        model=self.llm_model,
-                        max_tokens=600,
-                        temperature=0.2,
-                    )
-                    raw2 = text_utils.pre_sanitize(
-                        resp2["choices"][0]["message"]["content"]
-                    )
-                    parsed = json_utils.extract_json_balanced(raw2)
-
-                if parsed is None:
-                    raise ValueError()
-
-                return parsed
-            except Exception as e:
-                print(repr(e))
-                return HTTPException(500, "Ошибка сервера")
+        return parsed
 
 
 setup_logging(logging.INFO)
-api_key = model_api_utils.get_api_key()
 
 
 async def generate_presentation(user_prompt: str, project_context: str, model: str):
     pipe = IntegratedPipeline(
-        api_key=api_key,
+        api_key=get_runtime_api_key(),
         embedding_model=settings.DEFAULT_EMBEDDING_MODEL,
-        llm_model=model,
+        llm_model=model or settings.DEFAULT_MODEL,
     )
     pipe.load_documents([project_context])
 
     for slide in pipe.run(user_prompt, project_context=project_context):
         title = slide.get("title", "Untitled")
         content = slide.get("content", "")
-        chunk = f"# {title}\n\n{content.strip()}\n\n"
-        yield chunk
+        yield f"# {title}\n\n{content.strip()}\n\n"
 
 
-def edit_one_slide(user_prompt, slide: dict, action: str, model: str) -> str:
+def edit_one_slide(user_prompt, slide: dict, action: ModelAction, model: str) -> str:
     pipe = IntegratedPipeline(
-        api_key=api_key,
+        api_key=get_runtime_api_key(),
         embedding_model=settings.DEFAULT_EMBEDDING_MODEL,
-        llm_model=settings.DEFAULT_MODEL,
+        llm_model=model or settings.DEFAULT_MODEL,
     )
-    content = pipe.regenerate_slide(user_prompt, slide, action).get("content", "")
-
-    return f"# {content}\n"
+    edited = pipe.regenerate_slide(user_prompt, slide, action)
+    title = edited.get("title", slide.get("title", ""))
+    content = edited.get("content", "")
+    return f"# {title}\n\n{content}\n"
diff --git a/backend/src/services/rust_sidecar_client.py b/backend/src/services/rust_sidecar_client.py
--- a/backend/src/services/rust_sidecar_client.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/services/rust_sidecar_client.py	2026-04-06 21:24:52.207456478 +0000
@@ -4,6 +4,8 @@
 
 from src.config import settings
 
+TEXT_FORMATS = {"md", "txt", "csv", "tsv", "json"}
+
 
 def rust_sidecar_enabled() -> bool:
     return settings.FEATURE_RUST_SIDECAR
@@ -13,10 +15,14 @@
     if not rust_sidecar_enabled():
         return None
 
+    ext = filename.rsplit(".", 1)[-1].lower() if "." in filename else ""
+    if ext not in TEXT_FORMATS:
+        return None
+
     try:
         resp = requests.post(
             f"{settings.RUST_SIDECAR_URL}/parse",
-            json={"filename": filename, "content": content.decode("utf-8", errors="ignore")},
+            json={"filename": filename, "content": content.decode("utf-8", errors="strict")},
             timeout=settings.RUST_SIDECAR_TIMEOUT,
         )
         if not resp.ok:
diff --git a/backend/src/services/tempfile_service.py b/backend/src/services/tempfile_service.py
--- a/backend/src/services/tempfile_service.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/services/tempfile_service.py	2026-04-06 21:24:52.211877056 +0000
@@ -1,10 +1,11 @@
-from pathlib import Path
+import time
 import uuid
+from pathlib import Path
 
 from fastapi import HTTPException
 
-from src.schemas.tempfile_schemas import TempfileInfoSchema
 from src.config import settings
+from src.schemas.tempfile_schemas import TempfileInfoSchema
 
 
 class _TempfileService:
@@ -19,27 +20,14 @@
 
         self._tempfile_dir.mkdir(exist_ok=True)
 
-    # async def cleanup_old_files(self) -> None:
-    #     while True:
-    #         now = dt.datetime.now()
-    #         expired_files = [
-    #             file_id
-    #             for file_id, file_info in self._files_registry.items()
-    #             if file_info.is_expired(now)
-    #         ]
-
-    #         for file_id in expired_files:
-    #             file_path = self._tempfile_dir / file_id
-    #             try:
-    #                 if file_path.exists():
-    #                     os.unlink(file_path)
-    #                 del self._files_registry[file_id]
-
-    #                 print(f"Удален временный файл: {file_id}")
-    #             except Exception as e:
-    #                 print(f"Tempfile error: {e}")
-
-    #         await asyncio.sleep()
+    def cleanup_old_files(self) -> None:
+        now = time.time()
+        for path in self._tempfile_dir.glob("*"):
+            try:
+                if path.is_file() and (now - path.stat().st_mtime) > self._cleanup_interval:
+                    path.unlink(missing_ok=True)
+            except Exception:
+                pass
 
     def save_file(self, content: bytes, file_ext: str) -> str:
         filename = f"{uuid.uuid4()}.{file_ext}"
@@ -50,7 +38,12 @@
         return filename
 
     def get_file(self, filename: str) -> Path:
-        file_path = self._tempfile_dir / filename
+        safe_name = Path(filename).name
+        file_path = (self._tempfile_dir / safe_name).resolve()
+        root = self._tempfile_dir.resolve()
+
+        if root not in file_path.parents and file_path != root:
+            raise HTTPException(400, "Некорректное имя файла")
 
         if not file_path.exists():
             raise HTTPException(404, "Файл не найден")
diff --git a/backend/src/utils/model_api_utils.py b/backend/src/utils/model_api_utils.py
--- a/backend/src/utils/model_api_utils.py	2026-04-03 19:35:33.000000000 +0000
+++ b/backend/src/utils/model_api_utils.py	2026-04-06 21:24:52.212269910 +0000
@@ -7,6 +7,7 @@
 from src.config import settings, model_settings
 from src.services.secret_manager import read_secret
 
+SESSION = requests.Session()
 _TOKEN_CACHE: dict[str, float | str] = {"access_token": "", "expires_at": 0.0}
 
 
@@ -30,7 +31,7 @@
         "Accept": "application/json",
         "RqUID": str(uuid.uuid4()),
     }
-    resp = requests.post(
+    resp = SESSION.post(
         settings.GIGACHAT_OAUTH_URL,
         headers=headers,
         data={"scope": settings.GIGACHAT_SCOPE},
@@ -60,111 +61,6 @@
     _TOKEN_CACHE["expires_at"] = expires_at
     return token
 
-_TOKEN_CACHE: dict[str, float | str] = {"access_token": "", "expires_at": 0.0}
-
-
-def _auth_header_value(auth_key: str | None = None) -> str:
-    value = (auth_key or settings.GIGACHAT_AUTH_KEY).strip()
-    if value.lower().startswith("basic "):
-        return value
-    return f"Basic {value}"
-
-
-def _get_gigachat_token(force_refresh: bool = False, auth_key: str | None = None) -> str:
-    now = time.time()
-    cached_token = str(_TOKEN_CACHE.get("access_token", ""))
-    expires_at = float(_TOKEN_CACHE.get("expires_at", 0.0))
-    if not force_refresh and cached_token and now < (expires_at - 30):
-        return cached_token
-
-    headers = {
-        "Authorization": _auth_header_value(auth_key),
-        "Content-Type": "application/x-www-form-urlencoded",
-        "Accept": "application/json",
-        "RqUID": str(uuid.uuid4()),
-    }
-    resp = requests.post(
-        settings.GIGACHAT_OAUTH_URL,
-        headers=headers,
-        data={"scope": settings.GIGACHAT_SCOPE},
-        timeout=20,
-        verify=settings.GIGACHAT_VERIFY_SSL,
-    )
-    if not resp.ok:
-        raise RuntimeError(f"GigaChat auth error {resp.status_code}: {resp.text}")
-
-    data = resp.json()
-    token = (data.get("access_token") or "").strip()
-    if not token:
-        raise RuntimeError(f"Missing access_token in GigaChat auth response: {data}")
-
-    expires_at_raw = data.get("expires_at")
-    expires_in_raw = data.get("expires_in")
-    expires_at = now + 30 * 60
-    if isinstance(expires_at_raw, (int, float)):
-        if expires_at_raw > 10_000_000_000:
-            expires_at = float(expires_at_raw) / 1000.0
-        else:
-            expires_at = float(expires_at_raw)
-    elif isinstance(expires_in_raw, (int, float)):
-        expires_at = now + float(expires_in_raw)
-
-    _TOKEN_CACHE["access_token"] = token
-    _TOKEN_CACHE["expires_at"] = expires_at
-    return token
-
-_TOKEN_CACHE: dict[str, float | str] = {"access_token": "", "expires_at": 0.0}
-
-
-def _auth_header_value(auth_key: str | None = None) -> str:
-    value = (auth_key or settings.GIGACHAT_AUTH_KEY).strip()
-    if value.lower().startswith("basic "):
-        return value
-    return f"Basic {value}"
-
-
-def _get_gigachat_token(force_refresh: bool = False, auth_key: str | None = None) -> str:
-    now = time.time()
-    cached_token = str(_TOKEN_CACHE.get("access_token", ""))
-    expires_at = float(_TOKEN_CACHE.get("expires_at", 0.0))
-    if not force_refresh and cached_token and now < (expires_at - 30):
-        return cached_token
-
-    headers = {
-        "Authorization": _auth_header_value(auth_key),
-        "Content-Type": "application/x-www-form-urlencoded",
-        "Accept": "application/json",
-        "RqUID": str(uuid.uuid4()),
-    }
-    resp = requests.post(
-        settings.GIGACHAT_OAUTH_URL,
-        headers=headers,
-        data={"scope": settings.GIGACHAT_SCOPE},
-        timeout=20,
-        verify=settings.GIGACHAT_VERIFY_SSL,
-    )
-    if not resp.ok:
-        raise RuntimeError(f"GigaChat auth error {resp.status_code}: {resp.text}")
-
-    data = resp.json()
-    token = (data.get("access_token") or "").strip()
-    if not token:
-        raise RuntimeError(f"Missing access_token in GigaChat auth response: {data}")
-
-    expires_at_raw = data.get("expires_at")
-    expires_in_raw = data.get("expires_in")
-    expires_at = now + 30 * 60
-    if isinstance(expires_at_raw, (int, float)):
-        if expires_at_raw > 10_000_000_000:
-            expires_at = float(expires_at_raw) / 1000.0
-        else:
-            expires_at = float(expires_at_raw)
-    elif isinstance(expires_in_raw, (int, float)):
-        expires_at = now + float(expires_in_raw)
-
-    _TOKEN_CACHE["access_token"] = token
-    _TOKEN_CACHE["expires_at"] = expires_at
-    return token
 
 def call_model(
     messages: list[dict],
@@ -187,7 +83,7 @@
         "Accept": "application/json",
     }
 
-    resp = requests.post(
+    resp = SESSION.post(
         settings.GIGACHAT_API_URL,
         headers=headers,
         json=payload,
@@ -197,7 +93,7 @@
     if resp.status_code == 401:
         refreshed = _get_gigachat_token(force_refresh=True, auth_key=api_key)
         headers["Authorization"] = f"Bearer {refreshed}"
-        resp = requests.post(
+        resp = SESSION.post(
             settings.GIGACHAT_API_URL,
             headers=headers,
             json=payload,
@@ -225,12 +121,15 @@
 
 
 def get_api_key(explicit: str | None = None) -> str:
-    key = (explicit or read_secret("GIGACHAT_AUTH_KEY", "GIGACHAT_AUTH_KEY_FILE") or settings.GIGACHAT_AUTH_KEY or "").strip()
+    key = (
+        explicit
+        or read_secret("GIGACHAT_AUTH_KEY", "GIGACHAT_AUTH_KEY_FILE")
+        or settings.GIGACHAT_AUTH_KEY
+        or ""
+    ).strip()
     key = _strip_invisible(key)
     if not key:
-        raise RuntimeError(
-            "API auth key required (set env GIGACHAT_AUTH_KEY)."
-        )
+        raise RuntimeError("API auth key required (set env GIGACHAT_AUTH_KEY).")
     if not key.isascii():
         raise RuntimeError("API key contains non-ASCII characters.")
     return key
@@ -243,7 +142,7 @@
         ch
         for ch in s
         if ch.isprintable()
-        and ch not in ("\u00a0", "\u2009", "\u200a", "\u202f", "\u2007", "\u2060")
+        and ch not in (" ", " ", " ", " ", " ", "⁠")
     )
-    s = s.replace("\u200b", "").replace("\u200c", "").replace("\u200d", "")
+    s = s.replace("​", "").replace("‌", "").replace("‍", "")
     return s
diff --git a/docker-compose.yml b/docker-compose.yml
--- a/docker-compose.yml	2026-04-03 19:35:33.000000000 +0000
+++ b/docker-compose.yml	2026-04-06 21:24:52.214352865 +0000
@@ -7,7 +7,6 @@
     volumes:
       - ./nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
       - ./nginx/conf.d:/etc/nginx/conf.d/prod:ro
-      - ./frontend/build:/usr/share/nginx/html:ro
       - ./certbot/conf:/etc/letsencrypt:ro
       - ./certbot/www:/var/www/certbot:ro
     depends_on:
@@ -22,11 +21,6 @@
       dockerfile: Dockerfile
     environment:
       NODE_ENV: production
-    volumes:
-      - ./frontend:/app
-      - /app/node_modules
-    ports:
-      - "${FRONTEND_PORT:-3000}:3000"
     networks:
       - app-network
 
@@ -37,6 +31,8 @@
       args:
         PRELOAD_MODEL_CACHE: "true"
     command: uvicorn src.main:app --host 0.0.0.0 --port ${BACKEND_INTERNAL_PORT:-8000}
+    env_file:
+      - ./backend/.env
     environment:
       ENVIRONMENT: production
       POSTGRES_USER: ${POSTGRES_USER:-postgres}
diff --git a/nginx/conf.d/prod.conf b/nginx/conf.d/prod.conf
--- a/nginx/conf.d/prod.conf	2026-04-03 19:35:33.000000000 +0000
+++ b/nginx/conf.d/prod.conf	2026-04-06 21:24:52.215196815 +0000
@@ -8,24 +8,18 @@
         try_files $uri =404;
     }
 
-    # Frontend (React)
     location / {
-        proxy_pass http://frontend:3000;
+        proxy_pass http://frontend:80;
         proxy_http_version 1.1;
-        proxy_set_header Upgrade $http_upgrade;
-        proxy_set_header Connection "upgrade";
         proxy_set_header Host $host;
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
         proxy_set_header X-Forwarded-Proto $scheme;
     }
 
-    # Backend API
     location /api/ {
-        proxy_pass http://backend:8000/api/;
+        proxy_pass http://backend:8000/;
         proxy_http_version 1.1;
-        proxy_set_header Upgrade $http_upgrade;
-        proxy_set_header Connection "upgrade";
         proxy_set_header Host $host;
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
diff --git a/nginx/nginx.prod.conf b/nginx/nginx.prod.conf
--- a/nginx/nginx.prod.conf	2026-04-03 19:35:33.000000000 +0000
+++ b/nginx/nginx.prod.conf	2026-04-06 21:24:52.214602161 +0000
@@ -12,9 +12,8 @@
     include       /etc/nginx/mime.types;
     default_type  application/octet-stream;
 
-    # Определение upstream серверов для prod
     upstream frontend {
-        server frontend:3000;
+        server frontend:80;
     }
 
     upstream backend {
@@ -31,4 +30,4 @@
     keepalive_timeout  65;
 
     include /etc/nginx/conf.d/prod/*.conf;
-} 
\ No newline at end of file
+}
